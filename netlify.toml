[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback: let client-side routing handle unknown paths.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
